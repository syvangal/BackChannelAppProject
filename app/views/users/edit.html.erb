<h1>Editing user</h1>

<%= render 'form' %>


 <% if(session[:role] == 'admin') %>
   <%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
    <% else %>
   <%= link_to 'Login Page', logins_path %>
<% end %>